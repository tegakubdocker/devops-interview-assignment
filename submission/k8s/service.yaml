apiVersion: v1
kind: Service
metadata:
  name: video-processor
  namespace: video-analytics
  labels:
    app: video-processor
spec:
  type: ClusterIP
  selector:
    app: video-processor
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP