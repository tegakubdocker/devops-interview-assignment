apiVersion: v1
kind: Service
metadata:
  name: inference-api
  namespace: video-analytics
spec:
  type: ClusterIP
  selector:
    app: inference-api
    tier: backend
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
